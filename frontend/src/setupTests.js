// Charge automatiquement par Create React App avant chaque suite.
//
// jest-dom ajoute les assertions qui parlent le DOM : toBeInTheDocument,
// toHaveTextContent, toBeVisible. Sans lui, un test d'interface se resume a
// comparer des chaines, ce qui echoue mal et se lit encore plus mal.
import "@testing-library/jest-dom";
